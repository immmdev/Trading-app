npm i body-paser cor->for cross origin resource sharing to establish connection between frontend and the backend
npm i body-paser cors express mongoose passport passport-local passport-local-mongoose dotenv
PORT=3001; npm start  in the window shell
